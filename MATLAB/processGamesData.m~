%objectiveMeasures = zeros(6,6,5)
participant = 7

numGoalGets = zeros(6);
numCrashes = crashes;
numInterventions = flags;
numFalseFlags = falseFlags;
score = scored;

for game = 0:5
    [goals, crashes, flags, falseFlags, scored] = processGameData(participant,game)
    %objectiveMeasures(participant-6,game+1,:) = [goals, crashes, flags, falseFlags, scored]
    numGoalGets(game+1) = goals;
    numCrashes(game+1) = crashes;
    numInterventions(pgame+1) = flags;
    numFalseFlags(game+1) = falseFlags;
    score(game+1) = scored;
end